<%= form_for(microentrada) do |f| %>
  <% if microentrada.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(microentrada.errors.count, "error") %> prohibited this microentrada from being saved:</h2>

      <ul>
      <% microentrada.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :contenido %>
    <%= f.text_area :contenido %>
  </div>

  <div class="field">
    <%= f.label :usuario_id %>
    <%= f.number_field :usuario_id %>
  </div>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
