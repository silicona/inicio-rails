<%= form_for(usuario_actual.relaciones_activas.find_by(seguido_id: @usuario.id), 
																											 html: { method: :delete},
																											 remote: true) do |f| %>
	<%= f.submit "Dejar de seguir", class: "btn" %>
<% end %>